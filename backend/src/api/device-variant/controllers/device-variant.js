'use strict';

/**
 * device-variant controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::device-variant.device-variant');
